fillClientsTable()
fillInterventionsTable()
fillIntervenantsTable()





























{/* <script>// Authentification
      document.querySelector('form').addEventListener('submit', function(event) {
      event.preventDefault();
      
      let username = document.querySelector('.input-field input[type="text"]').value;
      let password = document.querySelector('.input-field input[type="password"]').value;
      
      if (username === "admin" && password === "admin") {
          alert("Connexion réussie !");
          window.location.href = "dashboard.html"; // Redirection vers une autre page
      } else {
          alert("Nom d'utilisateur ou mot de passe incorrect");
      }
  });
  </script> */}